services:
  express-typescript-docker:
    environment:
      - NODE_ENV=development
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
    container_name: ecommerce-api
    expose:
      - '3000'
    ports:
      - '3000:3000'
    command: npm run dev